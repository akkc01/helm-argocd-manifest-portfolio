apiVersion: v1
kind: Service
metadata:
  name: akkc-service

spec:
  type: {{ .Values.service.type }}

  selector:
    app: {{ .Values.labels.app }}

  ports:
  - protocol: TCP
    port: {{ .Values.service.port }}
    targetPort: 80